# Copie para .env.deploy e preencha (arquivo .env.deploy NÃO vai para o Git).
# Conta: tanolinkweb@gmail.com / GitHub: tanolinkweb-cmd / Vercel team: tano-link

# GitHub → Settings → Developer settings → Personal access tokens (classic)
# Escopo: repo
GITHUB_TOKEN=

# Vercel → Account Settings → Tokens → Create
# Escopo: Full Account (ou o time tano-link)
VERCEL_TOKEN=
